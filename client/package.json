{
  "name": "client",
  "version": "0.0.0",
  "private": true,
  "//": "http-server library (backup) ---> start: http-server ./dist -p 0   ... but CAREFUL: this activates process.env which is wrong!",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "serve": "http-server ./dist -p 0",
    "railway-start": "vite build && http-server ./dist -p 0"
  },
  "dependencies": {
    "axios": "^1.3.1",
    "dotenv": "^16.0.3",
    "http-server": "^14.1.1",
    "socket.io-client": "^4.7.1",
    "vue": "^3.2.45",
    "vue-router": "^4.1.6",
    "vuetify": "^3.1.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "vite": "^4.0.0"
  }
}
